<h1 class="font-bold text-4xl">New page</h1>
<%= render partial: 'form', locals: { page: @page } %>
<%= link_to 'All Pages', admin_pages_path, class: 'ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium' %>

<div class="md:w-2/3 mx-auto w-full">
  <h1 class="font-bold text-4xl">Editing page</h1>
  <%= render partial: 'form', locals: { page: @page } %>
  <%= link_to 'Live', page_path(@page), class: 'ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium', target: '_blank' %>
  <%= link_to 'Manage Pages', admin_pages_path, class: 'ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium' %>
</div>

<%= form_with model: [:admin, page], class: 'contents', data: { turbo: false } do |f| %>
  <% if page.errors.any? %>
    <div class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3" id="error_explanation">
      <h2>
        <%= pluralize(page.errors.count, "error") %>
        prohibited this page from being saved:
      </h2>
      <ul>
        <% page.errors.each do |error| %>
          <li>
            <%= error.full_message %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="my-5">
    <%= f.label :title %>
    <%= f.text_field :title, class: 'block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full' %>
  </div>
  <div class="my-5 grid grid-cols-1 gap-x-12 gap-y-6 md:gap-y-16 md:grid-cols-2">
    <div class="flex flex-col">
      <%= f.label :keywords %>
      <%= f.text_field :keywords, class: 'block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full' %>
    </div>
    <div class="flex flex-col">
      <%= f.label :link_label %>
      <%= f.text_field :link_label, class: 'block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full' %>
    </div>
  </div>
  <div class="my-5 grid grid-cols-2 gap-x-12 gap-y-16 md:grid-cols-3">
    <div class="flex flex-col">
      <%= f.label 'Welcome Page?' %>
      <%= f.check_box :welcome_content, class: 'block mt-2 h-5 w-5' %>
    </div>
    <div class="flex flex-col">
      <%= f.label 'Show in Navigation?' %>
      <%= f.check_box :navigation, class: 'block mt-2 h-5 w-5' %>
    </div>
    <div class="flex flex-col">
      <%= f.label 'Navigation Position (left to right)', class: 'w-96 md:w-auto' %>
      <%= f.number_field :position, class: 'block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full lg:w-24' %>
    </div>
  </div>
  <div class="my-5">
    <%= f.label :body %>
    <%= f.rich_text_area :body, class: 'block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 h-full w-full' %>
  </div>
  <div class="inline">
    <%= f.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>

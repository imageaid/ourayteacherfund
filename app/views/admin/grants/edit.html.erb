<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">
    Editing
    <%= link_to @grant.name, admin_grant_path(@grant), class: "inline-link" %>
  </h1>
  <%= render "form", grant: @grant %>
</div>
